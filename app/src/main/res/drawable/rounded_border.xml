<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/app_main_item" /> <!-- Màu nền -->
    <corners android:radius="8dp" /> <!-- Bán kính bo tròn -->
    <stroke
        android:width="1dp"
        android:color="#000000" /> <!-- Viền -->
</shape>
